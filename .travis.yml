sudo: enabled
addons:
    apt:
        packages:
            - ruby
            - gem
            - bundle
script:
#    - sudo apt install jekyll
    # - gem install jekyll jekyll-paginate
    # - jekyll build
    - bundle update
    - bundle exec jekyll build